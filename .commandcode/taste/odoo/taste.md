# odoo
- Use declarative XML view inheritance (like sales views) over direct DB manipulation. Follow Odoo best practices. Confidence: 0.70
- In QWeb templates, format monetary amounts using t-options="{'widget': 'monetary', 'display_currency': currency_var}" — never use format_amount() which doesn't exist in QWeb context. Confidence: 0.70
- In Odoo 19 QWeb xpath expressions, use hasclass('class-name') instead of contains(@class, 'class-name') for class-based element selection — Odoo flags contains(@class) as error-prone. Confidence: 0.65
- In Odoo 19 OWL template inheritance (t-inherit-mode="extension"), XPath selectors cannot target virtual <t> elements or t-* attributes — <t> nodes are compiled away and t-esc/t-foreach directives don't exist as DOM attributes. Use selectors with real DOM elements (class, name, id) that survive OWL compilation. Confidence: 0.75

- In Odoo QWeb, `t-set` variables are **never shared** between separate `<xpath>` blocks — even a separate template inheritance targeting `/t` does NOT make variables globally available across xpath blocks. The only reliable approach is to avoid `t-set` entirely for values needed across multiple xpaths; instead inline the lookup directly at each usage site (e.g., `env.ref('base.USD', raise_if_not_found=False)` or `env['res.currency'].search([('name','=','COP')], limit=1)`). Confidence: 0.80
- In Odoo QWeb report templates, reference standard currencies (like USD) via `env.ref('base.USD', raise_if_not_found=False)` directly in t-options—no t-set variables, no separate template inheritance. For non-standard currencies that may lack an xml_id (e.g., COP), fall back with `env.ref('base.COP', raise_if_not_found=False) or env['res.currency'].sudo().search([('name','=','COP')], limit=1)`. Confidence: 0.65
- When using Odoo 19's patch() on an OWL component, prefer super.method(...) over capturing BaseClass.prototype.method — patch() sets up the prototype chain via Object.setPrototypeOf, so super resolves correctly through all patches. Capturing the method before patching breaks when multiple modules patch the same method. Confidence: 0.80
- When overriding Odoo model methods that the core framework calls (like _prepare_account_move_line), always maintain the original method signature exactly — changing parameter names or removing parameters that the core passes positionally will cause TypeError on other servers. Match the parent class signature precisely, do NOT use generic *args, **kwargs as a workaround. Confidence: 0.80
- In Odoo 19 (17+), the "attrs" attribute is no longer supported in XML views. Replace attrs="{'required': [...], 'invisible': [...]}" with direct field attributes: invisible="condition" and required="condition" using Python-style expressions (e.g., invisible="not bcv_manual_rate_active"). Confidence: 0.80
- In Odoo 19, db_name from odoo_config can be a list. Always wrap with isinstance(db_name, list) check and extract db_name[0] before passing to Registry(db_name). Confidence: 0.65
- When displaying COP amounts in QWeb/website templates, use explicit string formatting like '$ {:,.0f} COP'.format(value) instead of the monetary widget — COP's $ symbol is identical to USD, causing ambiguity. Confidence: 0.70
- The bcv_rate_update_venezuela module should declare currency_rate_update_colombia as a dependency in __manifest__.py so the Colombia TRM provider gets auto-installed and COP rates are available. Confidence: 0.60
- In Odoo 19, stock routes with product_selectable=False (like Manufacture: mrp.route_warehouse0_manufacture) cannot be assigned to products via route_ids — they apply at warehouse level. Only product_selectable=True routes (like MTO: stock.route_warehouse0_mto) appear in the product form and CSV export/import for route_ids. Confidence: 0.70
- In XML form views, always add an explicit currency label (e.g., <span class="text-muted small mx-1">COP</span>) after monetary widget fields when multiple currencies share the same symbol ($) — otherwise users cannot distinguish amounts. Apply the same invisible attribute as the field itself. Confidence: 0.75
- For website templates and OWL components, ensure mobile-responsive spacing — use Bootstrap grid classes (row g-3, col-md-*, col-*) with proper breakpoints, avoid cramped layouts on small smartphones, and distribute spacing evenly to keep the UI user-friendly on narrow screens. Confidence: 0.85
- When inheriting Odoo views via XML with ref="module.external_id", always verify the external ID exists first (grep the target module's source, check Odoo's official view names, or reference known addons) — using assumed naming patterns like pos_session_kanban_view for point_of_sale without verification will cause module install failures. Confidence: 0.75
