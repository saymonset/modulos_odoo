<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="stock_quant_tree_view_usd" model="ir.ui.view">
        <field name="name">stock.quant.tree.usd.inherit</field>
        <field name="model">stock.quant</field>
        <field name="priority">99</field>
        <field name="inherit_id" ref="stock.view_stock_quant_tree_simple"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='product_uom_id']" position="after">
                <field name="currency_usd_id" column_invisible="True"/>
                <field name="unit_cost_usd"
                       widget="monetary"
                       options="{'currency_field': 'currency_usd_id'}"
                       optional="show"/>
                <field name="value_usd"
                       widget="monetary"
                       options="{'currency_field': 'currency_usd_id'}"
                       optional="show"/>
            </xpath>
        </field>
    </record>

</odoo>