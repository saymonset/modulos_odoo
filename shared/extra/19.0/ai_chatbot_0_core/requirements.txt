# Dependencias externas del módulo
pydantic>=2.0,<3.0
httpx>=0.27.0  # Si usas llamadas HTTP (ej. a n8n o OpenAI)
openai>=1.40,<2  # Cliente OpenAI (IA del chatbot: menú por rol, RAG, etc.)
python-dotenv>=1.0.0  # Para variables de entorno locales (opcional)